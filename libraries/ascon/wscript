def configure(cfg):
    # No special configuration needed for ASCON library
    pass

def build(bld):
    bld(
        features = 'c cxx',
        source   = ['aead.c', 'ascon_ctx.cpp'],  
        includes = ['.'],
        defines  = ['AP_MAVLINK_ENCRYPT=1'],
        name     = 'ascon'
    )